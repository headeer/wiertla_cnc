{% comment %}
  Wiertla Categories Filters Component
  A reusable component to display the category filters for the Wiertla CNC Shopify theme.
{% endcomment %}

<!-- Category Icons Per Tab -->
<div class="wiertla-categories__icons">
  <!-- WIERTŁA tab icons -->
  <div class="wiertla-categories__icon-item wiertla-tab-wiertla" data-category="koronkowe" data-tab="wiertla">
    <img src="{{ 'ico_big_koronkowe.svg' | asset_url }}" alt="Koronkowe" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">KORONKOWE</span>
  </div>
  <div class="wiertla-categories__icon-item wiertla-tab-wiertla" data-category="plytkowe" data-tab="wiertla">
    <img src="{{ 'ico_big_plytkowe.svg' | asset_url }}" alt="Płytkowe" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">PŁYTKOWE</span>
  </div>
  <div class="wiertla-categories__icon-item wiertla-tab-wiertla" data-category="vhm" data-tab="wiertla">
    <img src="{{ 'ico_big_vhm.svg' | asset_url }}" alt="VHM" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">VHM</span>
  </div>
  <div class="wiertla-categories__icon-item wiertla-tab-wiertla" data-category="sandvik" data-tab="wiertla">
    <img src="{{ 'ico_big_sandvik.svg' | asset_url }}" alt="Sandvik 880" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">SANDVIK 880</span>
  </div>
  <div class="wiertla-categories__icon-item wiertla-tab-wiertla" data-category="ksem" data-tab="wiertla">
    <img src="{{ 'ico_big_ksem.svg' | asset_url }}" alt="KSEM" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">KSEM</span>
  </div>
  <div class="wiertla-categories__icon-item wiertla-tab-wiertla" data-category="amec" data-tab="wiertla">
    <img src="{{ 'ico_big_amec.svg' | asset_url }}" alt="AMEC" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">AMEC</span>
  </div>
  <!-- PŁYTKI tab icons -->
  <div class="wiertla-categories__icon-item wiertla-tab-plytki" data-category="wcmx" data-tab="plytki">
    <img src="{{ 'ico_wcmx.jpeg' | asset_url }}" alt="WCMX" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">WCMX</span>
  </div>
  <div class="wiertla-categories__icon-item wiertla-tab-plytki" data-category="lcmx" data-tab="plytki">
    <img src="{{ 'ico_lcmx-881.svg' | asset_url }}" alt="LCMX" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">LCMX</span>
  </div>
  <div class="wiertla-categories__icon-item wiertla-tab-plytki" data-category="811" data-tab="plytki">
    <img src="{{ 'ico_lcmx-881.svg' | asset_url }}" alt="811" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">811</span>
  </div>
  <div class="wiertla-categories__icon-item wiertla-tab-plytki" data-category="dft" data-tab="plytki">
    <img src="{{ 'ico_dft.svg' | asset_url }}" alt="DFT" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">DFT</span>
  </div>
  <div class="wiertla-categories__icon-item wiertla-tab-plytki" data-category="880" data-tab="plytki">
    <img src="{{ 'ico_880.svg' | asset_url }}" alt="880" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">880</span>
  </div>
  <div class="wiertla-categories__icon-item wiertla-tab-plytki" data-category="wogx" data-tab="plytki">
    <img src="{{ 'ico_wogx.svg' | asset_url }}" alt="WOGX" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">WOGX</span>
  </div>
  <div class="wiertla-categories__icon-item wiertla-tab-plytki" data-category="spgx" data-tab="plytki">
    <img src="{{ 'ico_spgx.svg' | asset_url }}" alt="SPGX" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">SPGX</span>
  </div>
  <div class="wiertla-categories__icon-item wiertla-tab-plytki" data-category="p284" data-tab="plytki">
    <img src="{{ 'ico_p284.svg' | asset_url }}" alt="P284" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">P284</span>
  </div>
  <!-- KORONKI tab icons -->
  <div class="wiertla-categories__icon-item wiertla-tab-koronki" data-category="ksem" data-tab="koronki">
    <img src="{{ 'ico_ksem.svg' | asset_url }}" alt="KSEM" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">KSEM</span>
  </div>
  <div class="wiertla-categories__icon-item wiertla-tab-koronki" data-category="idi" data-tab="koronki">
    <img src="{{ 'ico_idi.svg' | asset_url }}" alt="IDI" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">IDI</span>
  </div>
  <div class="wiertla-categories__icon-item wiertla-tab-koronki" data-category="p600" data-tab="koronki">
    <img src="{{ 'ico_p600.svg' | asset_url }}" alt="P600" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">P600</span>
  </div>
  <div class="wiertla-categories__icon-item wiertla-tab-koronki" data-category="icm" data-tab="koronki">
    <img src="{{ 'ico_icm.svg' | asset_url }}" alt="ICM" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">ICM</span>
  </div>
  <div class="wiertla-categories__icon-item wiertla-tab-koronki" data-category="icp" data-tab="koronki">
    <img src="{{ 'ico_icp.svg' | asset_url }}" alt="ICP" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">ICP</span>
  </div>
  <div class="wiertla-categories__icon-item wiertla-tab-koronki" data-category="870" data-tab="koronki">
    <img src="{{ 'ico_870.svg' | asset_url }}" alt="870" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">870</span>
  </div>
  <div class="wiertla-categories__icon-item wiertla-tab-koronki" data-category="amec" data-tab="koronki">
    <img src="{{ 'ico_amec.svg' | asset_url }}" alt="AMEC" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">AMEC</span>
  </div>
  <div class="wiertla-categories__icon-item wiertla-tab-koronki" data-category="ktip" data-tab="koronki">
    <img src="{{ 'ico_ktip.svg' | asset_url }}" alt="KTIP" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">ktip</span>
  </div>
  <!-- WSZYSTKIE (all) icon -->
  <div class="wiertla-categories__icon-item wiertla-tab-wiertla wiertla-tab-plytki wiertla-tab-koronki" data-category="wszystkie" data-tab="all">
    <img src="{{ 'ico_big_all.svg' | asset_url }}" alt="Wszystkie" class="wiertla-categories__icon" width="122" height="122">
    <span class="wiertla-categories__icon-label">WSZYSTKIE</span>
  </div>
</div>
<div class="wiertla-categories__filters desktop">
  <div class="wiertla-categories__filters-left">
    <button class="wiertla-categories__filter-button active" data-filter="wszystkie">Wszystkie</button>
    
    <!-- Typ wiertła filter (only for wiertla tab) -->
    <select class="wiertla-categories__filter wiertla-filter-type" data-filter="typ" data-tab="wiertla">
      <option value="">Typ wiertła</option>
      <option value="koronkowe">Koronkowe</option>
      <option value="plytkowe">Płytkowe</option>
      <option value="vhm">VHM</option>
      <option value="sandvik">Sandvik 880</option>
      <option value="ksem">KSEM</option>
      <option value="amec">AMEC</option>
    </select>
    
    <!-- Rodzaj filter for plytki tab (should appear in same position as Typ wiertła) -->
    <select class="wiertla-categories__filter wiertla-filter-rodzaj" data-filter="rodzaj" data-tab="plytki" style="display:none;">
      <option value="">Rodzaj</option>
      <option value="wcmx">WCMX</option>
      <option value="lcmx">LCMX</option>
      <option value="811">811</option>
      <option value="dft">DFT</option>
      <option value="880">880</option>
      <option value="wogx">WOGX</option>
      <option value="spgx">SPGX</option>
      <option value="p284">P284</option>
    </select>
    
    <!-- Rodzaj filter for koronki tab (should appear in same position as Typ wiertła) -->
    <select class="wiertla-categories__filter wiertla-filter-rodzaj" data-filter="rodzaj" data-tab="koronki" style="display:none;">
      <option value="">Rodzaj</option>
      <option value="ksem">KSEM</option>
      <option value="idi">IDI</option>
      <option value="p600">P600</option>
      <option value="icm">ICM</option>
      <option value="icp">ICP</option>
      <option value="870">870</option>
      <option value="amec">AMEC</option>
      <option value="ktip">ktip</option>
    </select>
    
    <select class="wiertla-categories__filter" data-filter="manufacturer">
      <option value="">Producent</option>
    </select>
  </div>

  <div class="wiertla-categories__filters-right">
  <button class="wiertla-categories__filter-btn mobile-only mobile">
      <img src="{{ 'icon_filter.svg' | asset_url }}" alt="Filter" width="24" height="24">
      <span>Filtruj</span>
    </button>
    <span class="wiertla-categories__per-page-label desktop">Pokaż</span>
    <div class="wiertla-categories__per-page-buttons">
      <button class="wiertla-categories__per-page-button active" data-value="20">20</button>
      <button class="wiertla-categories__per-page-button" data-value="50">50</button>
      <button class="wiertla-categories__per-page-button" data-value="100">100</button>
    </div>
    <span class="wiertla-categories__per-page-label">/ na stronę</span>
    
  </div>
</div>

<!-- Filter Modal -->
<div class="wiertla-categories__modal" id="filterModal">
  <div class="wiertla-categories__modal-content">
    <div class="wiertla-categories__modal-header">
      <h3>Filtruj wiertła w tabeli</h3>
      <button class="wiertla-categories__modal-close">&times;</button>
    </div>
    <div class="wiertla-categories__modal-body">
      <div class="wiertla-categories__modal-filters">
        <div class="wiertla-categories__modal-filter-group">
          <label>Typ wiertła</label>
          <select class="wiertla-categories__filter" data-filter="typ">
            <option value="">Wszystkie</option>
            <option value="koronkowe">Koronkowe</option>
            <option value="plytkowe">Płytkowe</option>
            <option value="vhm">VHM</option>
            <option value="sandvik">Sandvik 880</option>
            <option value="ksem">KSEM</option>
            <option value="amec">AMEC</option>
          </select>
        </div>
        <div class="wiertla-categories__modal-filter-group">
          <label>Wykaz koronek</label>
          <select class="wiertla-categories__filter" data-filter="crown">
            <option value="">Wszystkie</option>
            <option value="KK">KK</option>
            <option value="KI">KI</option>
            <option value="KS">KS</option>
            <option value="KT">KT</option>
            <option value="KA">KA</option>
          </select>
        </div>
        <div class="wiertla-categories__modal-filter-group">
          <label>Producent</label>
          <select class="wiertla-categories__filter" data-filter="manufacturer">
            <option value="">Wszystkie</option>
          </select>
        </div>
      </div>
    </div>
    <div class="wiertla-categories__modal-footer">
      <button class="wiertla-categories__modal-apply">Zastosuj</button>
      <button class="wiertla-categories__modal-reset">Resetuj</button>
    </div>
  </div>
</div>

<script>
  
  document.addEventListener('DOMContentLoaded', function() {
    
    // Add event listeners to language buttons
    document.querySelectorAll('.wiertla-categories__language-btn').forEach(btn => {
      btn.addEventListener('click', function() {
        const lang = this.getAttribute('data-lang');
        if (window.WiertlaCNC && window.WiertlaCNC.changeLanguage) {
          window.WiertlaCNC.changeLanguage(lang);
        }
      });
    });

    // Modal functionality
    const modal = document.getElementById('filterModal');
    const filterBtn = document.querySelector('.wiertla-categories__filter-btn');
    const closeBtn = document.querySelector('.wiertla-categories__modal-close');
    const applyBtn = document.querySelector('.wiertla-categories__modal-apply');
    const resetBtn = document.querySelector('.wiertla-categories__modal-reset');

    if (filterBtn && modal) {
      filterBtn.addEventListener('click', function() {
        modal.classList.add('active');
        document.body.style.overflow = 'hidden';
      });
    }

    if (closeBtn && modal) {
      closeBtn.addEventListener('click', function() {
        modal.classList.remove('active');
        document.body.style.overflow = '';
      });
    }

    if (applyBtn) {
      applyBtn.addEventListener('click', function() {
        // Apply filters logic here
        modal.classList.remove('active');
        document.body.style.overflow = '';
      });
    }

    if (resetBtn) {
      resetBtn.addEventListener('click', function() {
        // Reset filters logic here
        const filters = modal.querySelectorAll('.wiertla-categories__filter');
        filters.forEach(filter => {
          filter.value = '';
        });
      });
    }

    // Close modal when clicking outside
    if (modal) {
      modal.addEventListener('click', function(e) {
        if (e.target === modal) {
          modal.classList.remove('active');
          document.body.style.overflow = '';
        }
      });
    }

    // Tab-specific producer filter logic
    function updateProducerOptions(tabType) {
      document.querySelectorAll('.wiertla-categories__filter[data-filter="manufacturer"]').forEach(function(select) {
        select.querySelectorAll('option').forEach(function(opt) {
          if (!opt.className || opt.value === "") {
            opt.style.display = '';
            return;
          }
          if (
            (tabType === 'wiertla' && opt.classList.contains('producer-wiertla')) ||
            (tabType === 'plytki' && opt.classList.contains('producer-plytki')) ||
            (tabType === 'koronki' && opt.classList.contains('producer-koronki'))
          ) {
            opt.style.display = '';
          } else {
            opt.style.display = 'none';
          }
        });
        // Reset value if current option is hidden
        if (select.selectedOptions.length && select.selectedOptions[0].style.display === 'none') {
          select.value = '';
        }
      });
    }

    // Listen for tab changes
    function setupProducerTabSync() {
      document.querySelectorAll('.wiertla-categories__tab').forEach(function(tab) {
        tab.addEventListener('click', function() {
          var tabType = this.getAttribute('data-tab-type');
          updateProducerOptions(tabType);
        });
      });
      // On page load, set correct options
      var activeTab = document.querySelector('.wiertla-categories__tab.active');
      if (activeTab) {
        updateProducerOptions(activeTab.getAttribute('data-tab-type'));
      }
    }
    document.addEventListener('DOMContentLoaded', setupProducerTabSync);

    // Typ/Rodzaj Filter (tab-specific)
    function updateTypeRodzajDropdown(tabType) {
      
      document.querySelectorAll('.wiertla-filter-type, .wiertla-filter-rodzaj').forEach(function(select) {
        const selectTab = select.getAttribute('data-tab');
        const selectClass = select.className;
        
        if (selectTab === tabType) {
          select.style.display = '';
        } else {
          select.style.display = 'none';
          select.value = '';
        }
      });
    }
    
    // Function to update active icon based on filter selection
    function updateActiveIcon(filterValue, tabType) {
      
      // Remove active class from all icons
      document.querySelectorAll('.wiertla-categories__icon-item').forEach(function(icon) {
        icon.classList.remove('active');
      });
      
      if (filterValue && filterValue !== '') {
        // Convert filter value to match data-category format
        const categoryValue = filterValue.toLowerCase();
        
        // Find the corresponding icon and make it active
        let targetIcon = null;
        
        // For wiertla tab, look for exact match
        if (tabType === 'wiertla') {
          targetIcon = document.querySelector(`.wiertla-categories__icon-item[data-category="${categoryValue}"]`);
        }
        // For plytki/koronki tabs, look for exact match
        else {
          targetIcon = document.querySelector(`.wiertla-categories__icon-item[data-category="${categoryValue}"]`);
        }
        
        if (targetIcon) {
          targetIcon.classList.add('active');
        }
      } else {
        // If no filter selected, make "wszystkie" active
        const allIcon = document.querySelector('.wiertla-categories__icon-item[data-category="wszystkie"]');
        if (allIcon) {
          allIcon.classList.add('active');
        }
      }
    }
    
    // Function to setup filter change listeners
    function setupFilterIconSync() {
      
      // Listen for changes on typ wiertła filter
      document.querySelectorAll('.wiertla-filter-type').forEach(function(select) {
        select.addEventListener('change', function() {
          const tabType = this.getAttribute('data-tab');
          const filterValue = this.value;
          updateActiveIcon(filterValue, tabType);
        });
      });
      
      // Listen for changes on rodzaj filters
      document.querySelectorAll('.wiertla-filter-rodzaj').forEach(function(select) {
        select.addEventListener('change', function() {
          const tabType = this.getAttribute('data-tab');
          const filterValue = this.value;
          updateActiveIcon(filterValue, tabType);
        });
      });
    }
    
    // Make function available globally for debugging
    window.testFilterSwitch = function(tabType) {
      updateTypeRodzajDropdown(tabType);
    };
    
    window.testIconSync = function(filterValue, tabType) {
      updateActiveIcon(filterValue, tabType);
    };
    
    
    const tabs = document.querySelectorAll('.wiertla-categories__tab');
    
    tabs.forEach(function(tab, index) {
     
      
      tab.addEventListener('click', function() {
        var tabType = this.getAttribute('data-tab-type');
        
        // CRITICAL: Reset selectedCategory when switching tabs
        window.selectedCategory = 'wszystkie';
        
        updateTypeRodzajDropdown(tabType);
        updateMobileTypeRodzajDropdown(tabType);
        
        // Reset to "wszystkie" when switching tabs
        updateActiveIcon('', tabType);
        
        // Also call the main reset function if available
        if (typeof window.resetCategoriesOnTabSwitch === 'function') {
          window.resetCategoriesOnTabSwitch(tabType);
        }
      });
    });
    
    // Mobile filter synchronization functions
    function updateMobileTypeRodzajDropdown(tabType) {
      
      document.querySelectorAll('.wiertla-filter-type, .wiertla-filter-rodzaj').forEach(function(select) {
        const selectTab = select.getAttribute('data-tab');
        const selectClass = select.className;
        
        if (selectTab === tabType) {
          select.style.display = '';
          
          // Safely check and update parent dropdown container
          const parentDropdown = select.closest('.wiertla-categories__mobile-filter-dropdown');
          if (parentDropdown) {
            parentDropdown.style.display = '';
          }
        } else {
          select.style.display = 'none';
          select.value = '';
          
          // Safely check and update parent dropdown container
          const parentDropdown = select.closest('.wiertla-categories__mobile-filter-dropdown');
          if (parentDropdown) {
            parentDropdown.style.display = 'none';
          }
        }
      });
    }
    
    function setupMobileFilterSync() {
      
      // Listen for changes on mobile typ wiertła filter
      document.querySelectorAll('.wiertla-categories__mobile-filter-select[data-filter="typ"]').forEach(function(select) {
        select.addEventListener('change', function() {
          const tabType = this.getAttribute('data-tab');
          const filterValue = this.value;
          updateActiveIcon(filterValue, tabType);
          
          // Sync with category selection
          if (typeof window.filterByCategory === 'function') {
            window.filterByCategory(filterValue || 'wszystkie');
          }
        });
      });
      
      // Listen for changes on mobile rodzaj filters
      document.querySelectorAll('.wiertla-categories__mobile-filter-select[data-filter="rodzaj"]').forEach(function(select) {
        select.addEventListener('change', function() {
          const tabType = this.getAttribute('data-tab');
          const filterValue = this.value;
          updateActiveIcon(filterValue, tabType);
          
          // Sync with category selection
          if (typeof window.filterByCategory === 'function') {
            window.filterByCategory(filterValue || 'wszystkie');
          }
        });
      });
      
      // Listen for changes on mobile manufacturer filter
      document.querySelectorAll('.wiertla-categories__mobile-filter-select[data-filter="manufacturer"]').forEach(function(select) {
        select.addEventListener('change', function() {
          const filterValue = this.value;
          
          // Apply manufacturer filter
          if (window.WiertlaCNC && window.WiertlaCNC.filters) {
            const manufacturerMapping = {
              'Sandvik': 'sandvik',
              'Walter': 'walter',
              'ISCAR': 'iscar',
              'KENNAMETAL': 'kennametal',
              'DSK': 'dsk',
              'AMEC': 'amec'
            };
            window.WiertlaCNC.filters.manufacturer = filterValue ? manufacturerMapping[filterValue] : '';
            
            // Apply filters
            if (typeof window.applyFilters === 'function') {
              window.applyFilters();
            }
          }
        });
      });
    }
    
    // Setup filter-icon synchronization
    setupFilterIconSync();
    
    // Setup mobile filter synchronization
    setupMobileFilterSync();
    
    // On page load
    var activeTab = document.querySelector('.wiertla-categories__tab.active');
    if (activeTab) {
      var initialTabType = activeTab.getAttribute('data-tab-type');
      updateTypeRodzajDropdown(initialTabType);
      updateMobileTypeRodzajDropdown(initialTabType);
      
   updateActiveIcon('', initialTabType);
    } else {
    }
    
    
  });
  
  // Also add immediate check in case DOM is already loaded
  if (document.readyState === 'loading') {
  } else {
    // Run the setup immediately if DOM is already loaded
    setTimeout(function() {
      const event = new Event('DOMContentLoaded');
      document.dispatchEvent(event);
    }, 100);
  }
</script>

<style>
.wiertla-categories__filter-btn {
  display: none;
  align-items: center;
  gap: 10px;
  padding: 10px 20px;
  background: #112832;
  border: none;
  border-radius: 30px;
  color: white;
  font-family: 'Montserrat', sans-serif;
  font-weight: 500;
  font-size: 14px;
  cursor: pointer;
  transition: all 0.3s ease;
}

.wiertla-categories__filter-btn:hover {
  background: #1a3a47;
}

.wiertla-categories__filter-btn img {
  width: 24px;
  height: 24px;
}

/* Modal styles */
.wiertla-categories__modal {
  display: none;
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.5);
  z-index: 1000;
}

.wiertla-categories__modal.active {
  display: flex;
  align-items: center;
  justify-content: center;
}

.wiertla-categories__modal-content {
  background: white;
  border-radius: 10px;
  width: 90%;
  max-width: 500px;
  max-height: 90vh;
  overflow-y: auto;
}

.wiertla-categories__modal-header {
  padding: 20px;
  border-bottom: 1px solid #eee;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.wiertla-categories__modal-header h3 {
  margin: 0;
  font-size: 18px;
  font-weight: 600;
}

.wiertla-categories__modal-close {
  background: none;
  border: none;
  font-size: 24px;
  cursor: pointer;
  padding: 0;
  color: #666;
}

.wiertla-categories__modal-body {
  padding: 20px;
}

.wiertla-categories__modal-filters {
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.wiertla-categories__modal-filter-group {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.wiertla-categories__modal-filter-group label {
  font-weight: 500;
  color: #333;
}

.wiertla-categories__modal-footer {
  padding: 20px;
  border-top: 1px solid #eee;
  display: flex;
  gap: 10px;
  justify-content: flex-end;
}

.wiertla-categories__modal-apply,
.wiertla-categories__modal-reset {
  padding: 10px 20px;
  border-radius: 5px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.3s ease;
}

.wiertla-categories__modal-apply {
  background: #112832;
  color: white;
  border: none;
}

.wiertla-categories__modal-reset {
  background: white;
  color: #112832;
  border: 1px solid #112832;
}

.wiertla-categories__modal-apply:hover {
  background: #1a3a47;
}

.wiertla-categories__modal-reset:hover {
  background: #f5f5f5;
}

@media (max-width: 768px) {
  .mobile-only {
    display: flex !important;
  }
  
  .wiertla-categories__filters-left {
    display: none;
  }
}




.wiertla-categories__icon-item.active .wiertla-categories__icon-label {
  font-weight: 600 !important;
}

/* Hover effect for better UX */
.wiertla-categories__icon-item {
  transition: all 0.3s ease !important;
  cursor: pointer !important;
}

.wiertla-categories__icon-item:hover span {
  text-decoration: none !important;
}
</style> 